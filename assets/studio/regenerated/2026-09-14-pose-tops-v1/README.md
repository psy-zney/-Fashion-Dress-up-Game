# Pose top sources — 2026-09-14

- Generation mode: built-in `image_gen`, one call per source.
- Canvas: 1024 × 1536, opaque white background.
- Denim mapping: hands on hips; raw source retained in `raw/`; selected uncut source scaled to 55% and positioned at x=230, y=101 to match the new model's upper-body scale.
- Mint mapping: arms crossed; raw source retained in `raw/`; selected uncut source scaled to 50% and positioned at x=256, y=142 to match the new model's upper-body scale.
- Masks/cutting: not performed; the user will cut the sources manually.
- Runtime integration: not performed.
