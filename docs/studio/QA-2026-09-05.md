# Kiểm tra cập nhật 05/09/2026

- TypeScript và production build: pass.
- E2E Chromium: 8/8 pass; sau lần dọn viền váy cuối, test phối boot/show-look được chạy lại và pass.
- Validator runtime: 14 món, 0 lỗi, 0 cảnh báo; có kiểm tra base boot không còn alpha chân dưới y995.
- Pipeline draft đầy đủ: prepare → validate → render chạy thành công trong `artifacts/studio-candidates/pipeline-check-20260905`; bản draft không được đưa vào web.
- Công cụ tạo prompt: đã chạy với intake mẫu, đầu ra `artifacts/studio-intake/example-20260905/`.

Ảnh bằng chứng ở `artifacts/studio-qa/`: `drag-sticker.png` (đang giữ chuột), `play-desktop.png`, `play-mobile.png`, `landing-background.png` (1920×1080), `completed-look-boots.png`. Ảnh phối pixel đầy đủ ở thư mục `assets/` bên trong QA; đã xem navy, xám và váy trắng cùng boot.

Những kiểm tra trực tiếp: ghost opacity=1 và cùng width/height với ảnh nguồn DOM; thả ngoài không mặc, thả vào stage mặc được; nút bán nguyệt desktop chạm cạnh tủ trong sai số <2px; menu mobile nằm trong viewport; váy xám không bị clip khi mang boot; đủ ba nhóm mới bật Show my look và hiệu ứng kết thúc được.

Giới hạn: không có baseline ảnh đã duyệt để khẳng định visual regression toàn bộ; không đo Core Web Vitals hoặc chứng nhận accessibility. Mobile hiện được kiểm tra bằng viewport Chromium, chưa phải cảm ứng trên thiết bị thật. Nguồn cũ vẫn có thể có viền sáng mảnh ở mức pixel. Hiệu ứng hoàn thiện là chuyển động 2D của cả stage, chưa phải rig bước đi/vẫy tay.

Nguồn ảnh không bị xoá trong lần sửa này. Các bản layers trước xử lý được giữ ở `assets/studio/archive/`.
